# DATABOSS system Python packages for Ubuntu 24.04.
# Install these system-wide with:
#   /usr/bin/python3 -m pip install --break-system-packages -r deploy/requirements-system.txt
# venv_bridge is created with --system-site-packages and deliberately inherits
# these packages. opencv-python-headless MUST stay at 4.13.0.92, not upgraded
# to match the main venv, or optical-flow results change.
numpy==2.4.6
opencv-python-headless==4.13.0.92
matplotlib==3.10.9
pillow==12.2.0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
packaging==26.2
python-dateutil==2.9.0.post0
uv==0.11.28
