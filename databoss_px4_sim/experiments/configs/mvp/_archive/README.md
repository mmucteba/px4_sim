# MVP Config Archive

These scenarios and batches are retired phase configs kept for reference.
They are no longer shown in the dashboard picker.

Every archived run carried its own `config.yaml` copy, so these files are
reference material rather than the source of record for completed runs.
